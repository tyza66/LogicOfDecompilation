找到弹出窗口那行汇编指令
右键、二进制、nop填充跳过即可

之后可以保存od修改后的程序
右键、复制到可执行文件、所有修改、全部复制
之后指到exe那个窗口，在有颜色的地方右键、保存文件即可导出exe

再进一步去除
直接ctrl+f搜索 使用push大法 先搜索push 10001
ctrl+l跳到下一个 找带520的
之后在下面找push 
凡是有id得到全部下断点
没有的是智慧窗口，不用下断点

直接重新运行程序看断点卡在哪 找到下面的call
判断一下这句call是不是就是弹出窗口的操作
这个单步是启动窗口的代码 那就从上一个窗口到call这里全部用nop覆盖

还可以直接用花式破解助手直接枚举窗口ID之后call窗口ID跳窗口
之后搜FF25 找到程序启动的时候 可以找到程序要启动的时候的程序段
ctrl+b 在hex中输入FF25进行搜索 之后ctrl+l往下找
第一个很可能就是启动窗口的id
看上面的push 里面很可能就是启动窗口的id 替换成我们想直接去的窗口即可 发现直接跳转到需要的窗口了
破解成功